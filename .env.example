# Optional overrides. docker-compose.yml already has runnable defaults.
# You do NOT need to put third-party model API URL/Key here; configure them in Learning Tracker → AI 模型配置.

ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
AI_HUB_ADMIN_TOKEN=dev-admin-token
AI_HUB_INTERNAL_TOKEN=dev-internal-token
AI_HUB_MASTER_KEY=dev-master-key-change-me
AI_HUB_TIMEOUT=300
AI_PROVIDER_TIMEOUT=180
AI_DIRECT_TIMEOUT=180

# Optional MySQL overrides. Default deployment reuses the VM's existing backend MySQL.
DB_HOST=host.docker.internal
DB_PORT=3306
DB_NAME=learning_tracker
DB_USER=lt_user
DB_PASSWORD=change-me

# Legacy direct-provider fallback for learning-tracker; normally leave empty.
OPENAI_BASE_URL=
OPENAI_MODEL=
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
